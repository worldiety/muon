// json contains a limited marshaller and unmarshaller which supports polymorphic serialization based
// on a jsonDiscriminator field. It is very limited in function and only supports a very small subset
// of field types and combinations.
package json
